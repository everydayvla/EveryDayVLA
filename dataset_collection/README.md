# Dataset Collection

This folder contains scripts to run dataset collection.

`dataset_collection_script.py` contains functions to generate tasks.

`dataset_collection_ik.py` and `dataset_collection_smooth.py` runs the robot for dataset collection

Make sure to only run `dataset_collection_smooth.py` with `6dof_control_smooth.ino`